update this because of backend changes on the reactionsummary

const topReactions = getTopReactions(post.reactionSummary);
const userReaction = post.reactionSummary.userReactionType;

<Heart
  className={`h-4 w-4 -ml-1 ${reactionStyles[r]} ${
    r === userReaction ? "border-2 border-blue-500" : i === 0 ? "" : "border border-white rounded-full"
  }`}
/>


apply this in dashboard only soon